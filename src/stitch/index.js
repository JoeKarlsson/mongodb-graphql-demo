import { app } from './app';
import {
	loginAnonymous,
	logoutCurrentUser,
	hasLoggedInUser,
	getCurrentUser,
} from './authentication';

export { app };
export { loginAnonymous, logoutCurrentUser, hasLoggedInUser, getCurrentUser };
